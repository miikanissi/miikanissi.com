# Go templates — Prettier's HTML formatter breaks Hugo/Go template syntax.
layouts/**/*.html

# Old theme CSS, replaced wholesale in the brand-system step; two of these
# files also embed Hugo template syntax that isn't valid CSS on its own.
assets/css/

public/
node_modules/
